fn main() -> num {
    array<num> arr_num = [ 0, 2, 4, 8, 16, 32 ];

    num: i = 0;

    while i < get_array_size(arr_num) {
        printf("arr_num[%i]=%li\n", i, get_num(arr_num, i));
        i = i + 1;
    }

    delete(arr_num); // delete automatically added by a translator soon :D

    return 0;
}