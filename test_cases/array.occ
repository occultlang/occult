fn main() -> num {
    array<num> arr_num;

    add_long(arr_num, 0); 
    add_long(arr_num, 2); 
    add_long(arr_num, 4); 
    add_long(arr_num, 8); 
    add_long(arr_num, 16);
    add_long(arr_num, 32);  

    num: i = 0;

    while i < get_size(arr_num) {
        printf("arr_num[%i]=%li\n", i, get_long(arr_num, i));
        i = i + 1;
    }

    free(arr_num);

    return 0;
}