fn main() -> num {
    array<num> arr_num;

    add_num(arr_num, 0); 
    add_num(arr_num, 2); 
    add_num(arr_num, 4); 
    add_num(arr_num, 8); 
    add_num(arr_num, 16);
    add_num(arr_num, 32);  

    num: i = 0;

    while i < get_array_size(arr_num) {
        printf("arr_num[%i]=%li\n", i, get_num(arr_num, i));
        i = i + 1;
    }

    delete(arr_num);

    return 0;
}