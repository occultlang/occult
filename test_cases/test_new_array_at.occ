fn main() {
    array: test = [1, 2, 3];

    print("size: %ld\n", test.size());

    test.add(4);

    print("new size: %ld\n", test.size());
    print("last element: %ld\n", test.get(test.size() - 1, num_t));

    test.set(test.size() - 1, 5);

    print("new last_element: %ld\n", test.get(test.size() - 1, num_t));
}

