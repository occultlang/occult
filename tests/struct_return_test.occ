include "../lib/stdio.occ"

struct point {
    i64 x;
    i64 y;
}

fn create_point(i64 x, i64 y) point {
    point p;
    p.x = x;
    p.y = y;
    return p;
}

fn main() {
    point p = create_point(100, 200);
    
    print_integer(p.x);
    print_string("\n");
    print_integer(p.y);
    print_string("\n");

    return 0;
}
