include "../lib/printf.occ"

fn variadic_sum(i64 count, ...) i64 {
    i64 total = 0;
    i64 i = 0;
    while i < count {
        total = total + __varargs[i];
        i = i + 1;
    }
    return total;
}

fn main() {
    print_integer(variadic_sum(3, 10, 20, 30));
    print_newline();
}
