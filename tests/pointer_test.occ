fn main() i64 {
    i64 orig = 10;

    i64* addr = @orig; // reference
    i64** addr2 = @addr;
    
    <-<-addr2 = 40; // how to dereference

    printn(addr); // raw address
    printn(<-addr); // 40
    printn(<-<-addr2); // 40
    
    return 0;
}
