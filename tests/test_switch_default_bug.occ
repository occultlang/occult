include "../std/stdio.occ"

enum Status {
  NaN,
  Alive,
  Dead
}

fn print_status(i64 s) {
  switch (s) {
    case Status::Alive: {
      std::printf("Alive\n");
    }
    case Status::Dead: {
      std::printf("Dead\n");
    }
    case Status::NaN:
    default: {
      std::printf("NaN\n");
    }
  }
}

fn main() i64 {
  // Test: case fallthrough to default
  i64 s1 = Status::NaN;
  std::printf("Testing NaN:\n");
  print_status(s1);

  i64 s2 = Status::Alive;
  std::printf("Testing Alive:\n");
  print_status(s2);

  i64 s3 = Status::Dead;
  std::printf("Testing Dead:\n");
  print_status(s3);

  // Test: unmatched value should also hit default via natural fallthrough
  std::printf("Testing unmatched:\n");
  print_status(99);

  std::printf("done\n");
  return 0;
}
