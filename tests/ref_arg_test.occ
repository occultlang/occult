/*
    Implicit return 0
    Defaults to i64 return type now
*/

include "../lib/stdio.occ"

fn add(i64@ to, i64 by) {
    $to = $to + by;
}

fn main() {
    i64 to = 10;
    i64 by = 50;

    add(to, by);

    print_integer(to); print_string("\n");
}