include "../std/stdio.occ"

fn main() i64 {
    // Edge case 1: empty switch
    i64 x = 5;
    switch (x) {
    }

    // Edge case 2: single case, no default
    switch (x) {
        case 5: {
            std::printf("five\n");
        }
    }

    // Edge case 3: only default
    switch (x) {
        default: {
            std::printf("default only\n");
        }
    }

    // Edge case 4: many cases
    i64 y = 7;
    switch (y) {
        case 0: {
            std::printf("0\n");
        }
        case 1: {
            std::printf("1\n");
        }
        case 2: {
            std::printf("2\n");
        }
        case 3: {
            std::printf("3\n");
        }
        case 4: {
            std::printf("4\n");
        }
        case 5: {
            std::printf("5\n");
        }
        case 6: {
            std::printf("6\n");
        }
        case 7: {
            std::printf("7\n");
        }
        default: {
            std::printf("other\n");
        }
    }

    // Edge case 5: negative case values
    i64 z = 0 - 1;
    switch (z) {
        case 0: {
            std::printf("zero\n");
        }
        default: {
            std::printf("negative\n");
        }
    }

    std::printf("done\n");
    return 0;
}
