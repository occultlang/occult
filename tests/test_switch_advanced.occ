include "../std/stdio.occ"

fn main() i64 {
    // Test case 1: match first case
    i64 a = 1;
    switch (a) {
        case 1: {
            std::printf("matched 1\n");
        }
        case 2: {
            std::printf("matched 2\n");
        }
        default: {
            std::printf("no match\n");
        }
    }

    // Test case 2: match default
    i64 b = 99;
    switch (b) {
        case 1: {
            std::printf("matched 1\n");
        }
        case 2: {
            std::printf("matched 2\n");
        }
        default: {
            std::printf("default hit\n");
        }
    }

    // Test case 3: switch on expression
    i64 c = 3;
    switch (c + 1) {
        case 4: {
            std::printf("3+1=4 works\n");
        }
        default: {
            std::printf("expr failed\n");
        }
    }

    // Test case 4: no default, no match
    i64 d = 10;
    switch (d) {
        case 1: {
            std::printf("should not print\n");
        }
        case 2: {
            std::printf("should not print\n");
        }
    }

    std::printf("done\n");
    return 0;
}
