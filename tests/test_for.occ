fn main() i64 {
  for i64 i = 0 when i < 10 do i = i + 1 {
     i32 int_max = 2147483647;
    //print("a\n"); PRINT CAUSED THIS WTF??!!! (need to rewrite) (was erroring in strlen in gdb so...)
  }

  for i64 i in arr {
    i32 body_decl = 1;
  }
  
  return int_max;
}
