include "../std/stdio.occ"

fn main() i64 {
  i64 y = 5;
  i64 x = 5;
  
  if x > y {
    std::print_string("x greater than y\n");
  }
  else if x < y {
    std::print_string("x less than y\n");
  }
  else {
    std::print_string("probably same lol\n");
  }

  return 0;
}
